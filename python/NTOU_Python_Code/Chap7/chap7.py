#For 迴圈可以讓程式將整個物件內的元素迭代
#➢可迭代物件(iterable object)可以是串列、元組、字典與集合或range( )
#for var in 可迭代物件:
#  程式碼區塊
# for 迴圈：用來逐一處理「可迭代物件」中的每個元素
# iterable（可迭代物件）包含：list, tuple, dict, set, string, range, enumerate...

drinks = ["coffee", "tea", "milk"]

# 逐一取出串列中的元素
for item in drinks:
    print(item)

# 迭代字串（逐字元）
for ch in "NTOU":
    print(ch)

# 使用 range() 迴圈
for i in range(5):
    print(i)

# 使用 enumerate() 同時取得索引與值
for index, value in enumerate(drinks):
    print(index, value)

#如果程式碼區塊只有一行
#✓for var in 可迭代物件：程式碼區塊

# ch7_6.py
players = ['Curry', 'Jordan', 'James', 'Durant', 'Obama']
for player in players[:3]:
    print(player)

#del —— 用「索引」刪除,remove() —— 用「元素值」刪除
fruits1 = ['蘋果', '香蕉', '西瓜', '水蜜桃', '百香果']
fruits2 = ['香蕉', '芭樂', '西瓜']
print("目前fruits2串列 : ", fruits2)
for fruit in fruits2[:]:#細節額外複製一個,不能在 for 迴圈的同時修改正在迭代的列表！
    if fruit in fruits1:
        fruits2.remove(fruit)
print(f"刪除 {fruit}")
print("最後fruits2串列 : ", fruits2)

#Python可以使用range( )函數產生一個等差級序列，我們又稱這等差級序列為可迭代物件(iterable object)，也可以稱是range物件
#range(start, stop, step)他是一種class
#total = sum(range(n + 1))
n=input()
n=n.split()
count =0
for i in n:
    if (int(i)>10):
        n[count]=10
    count+=1
print(n)
#刪除串列內所有元素，Python沒有提供刪除整個串列元素的方法，不過我們可以使用for迴圈完成此工作
# 列表生成式（list comprehension）
# 格式： [運算式 for 項目 in 可迭代物件]

# 1. 使用 list(range())
xlst = list(range(6))     # 直接產生 0~5
print(xlst)

# 2. 手動 append
xlst = []
xlst.append(0)
xlst.append(1)
xlst.append(2)
xlst.append(3)
xlst.append(4)
xlst.append(5)
print(xlst)

# 3. 使用 for 迴圈 append
xlst = []
for n in range(6):
    xlst.append(n)
print(xlst)

# 4. 使用列表生成式（最推薦）
xlst = [n for n in range(6)]
print(xlst)
#新串列 = [運算式 for 變數 in 可迭代物件 if 條件式]=
#新串列 = []
#for 項目 in 可迭代物件:
    #新串列.append(運算式)
# 使用列表生成式（list comprehension）找出 1~19 中所有的畢氏三元組
# 畢氏三元組：a、b、c 滿足 a^2 + b^2 = c^2（直角三角形三邊長）

x = [
    [a, b, c]                        # 每次找到符合條件的三元組，就把 [a, b, c] 加入列表
    for a in range(1, 20)            # 第一層迴圈：a 從 1 到 19
    for b in range(a, 20)            # 第二層迴圈：b 從 a 到 19（確保 a ≤ b，避免重複）
    for c in range(b, 20)            # 第三層迴圈：c 從 b 到 19（確保 b ≤ c，c 為最大邊）
    if a ** 2 + b ** 2 == c ** 2     # 過濾條件：必須符合畢氏定理 a² + b² = c²
]

print(x)   # 輸出所有符合條件的畢氏三元組

# ============================================================
# 列表生成式（List Comprehension）中的兩種 if 用法
# ============================================================

# ------------------------------------------------------------
# 情況 1：只有「篩選條件」（filter） → if 一定寫在最後
# ------------------------------------------------------------
# 語法：
#   [運算式 for 變數 in 可迭代物件 if 條件式]
#
# 說明：
#   - 這裡的 if 只是用來「過濾資料」
#   - 不是 if-else，因此必須放在 for 之後、整個式子的最後
# ------------------------------------------------------------

# ✔ 正確（篩選偶數）
example1 = [x for x in range(10) if x % 2 == 0]

# ❌ 錯誤（語法錯誤：過濾 if 不能放前面）
# example1 = [x if x % 2 == 0 for x in range(10)]

# ------------------------------------------------------------
# 情況 2：條件運算式（if-else） → if-else 必須寫在前面
# ------------------------------------------------------------
# 語法：
#   [運算式1 if 條件 else 運算式2 for 變數 in 可迭代物件]
#
# 說明：
#   - if-else 是一個「運算式」，屬於元素本身的值
#   - 因此必須寫在最前面（for 之前）
# ------------------------------------------------------------

# ✔ 正確（偶數→"偶數"，奇數→"奇數"）
example2 = ["偶數" if x % 2 == 0 else "奇數" for x in range(6)]

# ❌ 錯誤（語法錯誤：if-else 不能放在 for 之後）
# example2 = [for x in range(6) "偶數" if x % 2 == 0 else "奇數"]

# ------------------------------------------------------------
# ⭐ 最簡單的判斷方式
# ------------------------------------------------------------
# 若是「過濾」（filter） → if 在最後面
#       [x for x in lst if x > 0]
#
# 若是「條件決定值」（if-else） → if-else 在前面
#       [x if x > 0 else -1 for x in lst]
# ------------------------------------------------------------

# ------------------------------------------------------------
# 🔥 例子比較（非常重要）
# ------------------------------------------------------------

# 情況 1：篩選（filter）
# 只取出偶數
evens = [x for x in range(10) if x % 2 == 0]

# 情況 2：條件運算（if-else）
# 偶數 → True，奇數 → False
flags = [True if x % 2 == 0 else False for x in range(10)]
# ------------------------------------------------------------

# 印出結果（可自行測試）
print(example1)
print(example2)
print(evens)
print(flags)

colors = ["Red","Green","Blue"]
shapes = ["Circle","Square","Line"]
result=[[color,shape] for color in colors for shape in shapes]
print(result)

# for … else 的邏輯：
# 1. for 逐一檢查可迭代物件
# 2. 若 if 條件為 True → break → 不會執行 else
# 3. 若 for 迴圈都沒 break → 最後會執行 else

nums = [1, 3, 5, 7, 9]
target = 8

for n in nums:
    if n == target:         # 找到目標
        print("找到資料!")
        break               # 中斷迴圈（else 不會執行）
else:
    print("找不到資料")       # 只有沒 break 時才會執行

#else 只有在 for 迴圈「正常跑完」時才會執行
#如果 for 裡面發生 break，else 就不會執行

# 哨兵值（sentinel value）是一個用來結束輸入的特殊值
# 本程式的哨兵值是 0，輸入 0 表示「結束輸入」

n = int(input("請輸入一個值 : "))   # 第一次輸入
sum = 0                              # 用來累加總和
# while n 等同於 while n != 0
# 只要 n 不是 0，迴圈就會繼續
while n:
    sum += n                          # 累加輸入值
    n = int(input("請輸入一個值 : ")) # 再次讀下一筆輸入

# 當 n = 0 時跳出迴圈，印出總和
print("輸入總和 = ", sum)


drinks = ["coffee", "tea", "wine"]
# 解析enumerate物件
for drink in enumerate(drinks): # 數值初始是0
    print(drink)
for count, drink in enumerate(drinks):
    print(count, drink)
print("****************")
# 解析enumerate物件
for drink in enumerate(drinks, 10): # 數值初始是10
    print(drink)
for count, drink in enumerate(drinks, 10):
    print(count, drink)