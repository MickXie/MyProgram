# type( )函數 印出 變數的資料型態
x = 10
y = x / 3
print(x) 
print(type(x)) #<class 'int'>
print(y)
print(type(y)) #<class 'float'>
#整數使用時可以在數字中加上底線(_)
x =1_1_1 #x = 111
#Python具有簡單自動轉換能力
#2進位整數與函數bin( ) 可以將一般整數數字轉換為2進位。
#8進位整數與函數oct( )函數可以將一般數字轉換為8進位
#16進位整數與函數hex( )函數可以將一般數字轉換為16進位。
#int( )：將資料型態強制轉換為整數
#float( )：將資料型態強制轉換為浮點數
#abs( )：計算絕對值
#pow(x,y)：返回x的y次方
#round( )：這是採用演算法則的Bankers Rounding觀念
#當要四捨五入的位數「剛好是 .5」時： 若左邊是 奇數 → 進位（五入）若左邊是 偶數 → 不進位（五捨）
#字串(string)資料是指兩個單引號(‘)之間或是兩個雙引號(“)之間任意個數字元符號的資料 ->""裡面包含'的字串ex:"cc'dd"
#數學的運算子 ”+”，可以執行兩個字串相加，產生新的字串
#程式設計時如果字串長度多於一行，可以使用三個單引號(或是3個雙引號)將字串包夾即可
str1 = '''Silicon Stone Education is an unbiased organization
concentrated on bridging the gap ... '''
print(str1)
#\取消分行
str2 = '''Silicon Stone Education is an unbiased organization \
concentrated on bridging the gap ... '''
print(str2)
str3 = "Silicon Stone Education is an unbiased organization " \
"concentrated on bridging the gap ... "
print(str3)
# -----------------------------------------
# 常見跳脫字元（Escape Sequences）對照表
# -----------------------------------------
# 字元    Hex值   意義
# \'     27     單引號 (Single quote)
# \"     22     雙引號 (Double quote)
# \\     5C     反斜線 (Backslash)
# \a     07     響鈴 (Bell)
# \b     08     Backspace 鍵 (Backspace)
# \f     0C     換頁 (Form feed)
# \n     0A     換行 (New line)
# \o     08     八進位表示 (Octal value)
# \r     0D     游標移至最左位置 (Carriage return)
# \x     16     十六進位表示 (Hexadecimal value)
# \t     09     Tab 鍵效果 (Horizontal tab)
# \v     0B     垂直定位 (Vertical tab)
# -----------------------------------------
#str可以設空,也可以直接設字串,也可以直接用變數強制轉換
#x2 = x1 * 10字串*整數=字串複製效果
#Python沒有所謂的字元(character)資料,only  chr( x )傳回函數x值的ASCII或Unicode字元
#ord( x )：可以傳回函數字元參數x的Unicode碼值，如果是中文字也可傳回Unicode碼值
# -----------------------------------------------------------
# chr() 與 ord() 的差異與用途
# -----------------------------------------------------------
# ord()：將「字元」轉成對應的 Unicode 整數編碼
# chr()：將「Unicode 整數編碼」轉回對應的字元
# -----------------------------------------------------------

# 🔹 ord() — 字元 ➜ 整數 (Unicode / ASCII 編碼)
# 語法：ord(character)
# 範例：
print(ord('A'))   # 65
print(ord('a'))   # 97
print(ord('中'))  # 20013

# 🔹 chr() — 整數 ➜ 字元
# 語法：chr(number)
# 範例：
print(chr(65))     # 'A'
print(chr(97))     # 'a'
print(chr(20013))  # '中'

# 🔹 兩者互為反函數 (互相轉換)
c = 'Z'
print(ord(c))         # 90
print(chr(ord(c)))    # 'Z'   回到原本字元

# -----------------------------------------------------------
# 🧠 小整理：
# ord('A') = 65    chr(65) = 'A'
# ord('a') = 97    chr(97) = 'a'
# ord('0') = 48    chr(48) = '0'
# -----------------------------------------------------------
