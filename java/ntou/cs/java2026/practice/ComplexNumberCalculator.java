package ntou.cs.java2026.practice;

// Exercise 4 — 題目要求：
// 實作一個靜態方法，接收二維複數矩陣，計算結果：
//   1. 每一行（row）的所有複數加總
//   2. 所有行的加總結果相乘
//   3. 回傳最終乘積

public class ComplexNumberCalculator {

    // TODO: 實作 calculate(Complex[][] matrix)
    //
    // 驗證（丟出 IllegalArgumentException）：
    //   - matrix == null
    //   - matrix.length == 0（空矩陣）
    //   - 任何一行 row == null 或 row.length == 0
    //
    // 步驟 1：對每一行做加總，初始值 new Complex(0, 0)
    // 步驟 2：把所有行的加總結果相乘，初始值 new Complex(1, 0)
    // 步驟 3：回傳最終結果

    public static Complex calculate(Complex[][] matrix) {
        return null;
    }
}
